You are a senior full-stack engineer.

There is a mismatch issue in my MERN project (MusicFlow):

Frontend (user app) shows:
👉 "1 user listening right now" (Socket.IO real-time)

But Admin Analytics Dashboard shows:
👉 Active Users = 0

This means backend analytics API and real-time socket data are NOT synced.

---

# 🎯 TASK

Fix the ENTIRE admin analytics system so that all data is correct and consistent.

---

# 🔍 FIX THESE PROPERLY

## 1. Active Users Logic (CRITICAL)

* Ensure "Active Users" in admin analytics reflects REAL-TIME active listeners
* Use the SAME source of truth as Socket.IO
* If needed:

  * Store active listeners count in memory or Redis
  * Expose it via /api/admin/analytics

---

## 2. Backend Fix

* Update analyticsController.js:

  * Include correct activeUsers count (NOT static or wrong field)
  * Sync with Socket.IO connection tracking
* Ensure no mismatch like:

  * activeListeners vs activeUsers naming issues

---

## 3. Socket.IO Integration

* Ensure server tracks active users correctly:

  * On "user_started_listening" → increment
  * On "user_stopped_listening" / disconnect → decrement
* Maintain accurate count (no duplicates)

---

## 4. API Response Fix

Ensure /api/admin/analytics returns:
{
totalStreams,
activeUsers,
totalSongs,
totalAlbums,
topSongs,
topArtists,
recentActivity
}

---

## 5. Frontend Admin Fix

* Fix AdminAnalytics.jsx:

  * Match exact API fields
  * Remove wrong field usage
  * Ensure proper rendering of:

    * Active Users
    * Top Artists
    * Recent Activity

---

## 6. Top Artists + Recent Activity (EMPTY ISSUE)

* Fix backend queries:

  * Top artists aggregation
  * Recent activity data source (recentlyPlayed or logs)
* Ensure they return real data

---

## 7. FINAL VERIFICATION

After fixing:

* If 1 user is listening → Admin shows 1
* No fields show 0 incorrectly
* No undefined data
* All cards display correct values

---

# ⚠️ RULES

* Do NOT hardcode values
* Do NOT fake data
* Use real backend + socket data
* Keep logic simple and correct

---

# 📋 OUTPUT

* List what was wrong
* What you fixed
* Confirm everything is synced

---

Fix completely. No partial fixes.
